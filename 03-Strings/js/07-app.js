
// En CSS podria hacerse asi:
/*
p{
    text-transform: uppercase;
}
*/

// En JS
// .toUpperCase() te va a permitir cambiar un texto a todo mayusculas
const producto = 'Monitor 24 pulgadas ';
console.log(producto.toUpperCase() );

// .toLowerCase()
// Transformara todo a minusculas... ejemplo el email
console.log(producto.toLowerCase() );

// .toString()
// Transforma un numero a String
const cantidad = 200;
console.log(cantidad);
console.log(cantidad.toString());

// En Chrome un numero aparece en color azul en la consola
